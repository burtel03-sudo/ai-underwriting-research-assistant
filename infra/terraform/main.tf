# Placeholder. Production infrastructure as code (PRD §12: Terraform).
#
# Do not add real cloud resources here until the enterprise cloud provider,
# identity provider, and approved LLM providers are decided (PRD §18 open
# decisions). This file exists to establish the directory convention.

terraform {
  required_version = ">= 1.7"
}
